# 多面ニュース解説サイト - 仕様書 (CLAUDE.md)

## 1. プロジェクト概要

毎日の主要ニュースを、AIが完全自動で収集・クラスタリングし、トピックごとの「論点」を軸に賛否・立場をまとめて表示するサイト。

- 対象範囲: 幅広く総合(政治・社会・経済・国際・テック等)
- 運用: 完全自動(収集→要約→公開までAIパイプライン、人手チェックなし)
- 見せ方: 論点ベースで賛否・立場を整理(記事の並列表示ではない)
- 技術方針: React単一ファイルPWA + VercelサーバーレスAPIプロキシ(既存プロジェクトと同じ構成)

---

## 2. 全体アーキテクチャ

```
[RSS/News API] → [収集スクリプト] → [Claude APIでクラスタリング]
      → [Claude APIで論点抽出・賛否要約] → [JSON生成]
      → [GitHubリポジトリにコミット / Vercel KV保存] → [React PWAで表示]
```

- トリガー: GitHub Actions cron(1日2回: 朝7時・夜19時 JST目安)
- 実行環境: Node.js スクリプト
- データ永続化: リポジトリ内 `/data/YYYY-MM-DD.json` にコミット(苗字ルーツ帖と同様、DBレス構成)
- フロントは静的JSONをfetchして表示するだけのSPA

---

## 3. データソース(RSS)

初期選定。政治的立場・媒体傾向が偏らないよう意図的に多様化する。

| カテゴリ | メディア | 備考 |
|---|---|---|
| 公共 | NHKニュース | 中立基準の参照点 |
| 全国紙(保守傾向) | 産経新聞、読売新聞 | |
| 全国紙(リベラル傾向) | 朝日新聞、毎日新聞 | |
| 経済 | 日本経済新聞、Bloomberg Japan | |
| 国際 | Reuters Japan、AFPBB News | 海外視点の補完 |
| テック | ITmedia、TechCrunch Japan | カテゴリ「総合」の一部として |

**注記**: 各メディアのRSSは見出し・リード文のみ取得。本文スクレイピングは行わない(著作権対応)。

---

## 4. パイプライン詳細

### 4.1 収集(collect.js)
- 各RSSを取得し、`{title, summary, link, source, pubDate}` の配列に正規化
- 直近24時間分のみ対象

### 4.2 クラスタリング(cluster via Claude API)
- 全メディアの見出し+リード文をClaudeに渡し、「同一の出来事・トピックを報じている記事」をグルーピング
- 出力: `[{topicId, topicTitle, articles: [...]}]`
- 各グループは最低2媒体以上の言及があるものを採用(1媒体だけの話題は除外し、扱う量を絞る)

### 4.3 論点抽出・賛否要約(analyze via Claude API)
トピックごとに以下を生成する。プロンプト設計の要点:

- 「論点(何が争点か)」を1〜2個、短文で明示
- 各論点について「賛成/支持する立場の主張」と「反対/懸念する立場の主張」を、それぞれの媒体の論調から要約(**原文コピー禁止、必ず言い換え、各ソース2〜3文まで**)
- どのメディアの論調を参照したかを `sources` フィールドに明記(透明性の担保)
- 事実(何が起きたか)と論調(どう評価されているか)を明確に分離して出力させる

出力JSONスキーマ例:
```json
{
  "topicId": "2026-08-12-001",
  "topicTitle": "トピックの見出し",
  "facts": "客観的な出来事の要約(2〜3文、意見を含まない)",
  "issues": [
    {
      "issue": "論点の短文",
      "for": { "summary": "賛成側の要約", "sources": ["読売新聞", "産経新聞"] },
      "against": { "summary": "反対側の要約", "sources": ["朝日新聞", "毎日新聞"] }
    }
  ],
  "sourceLinks": [{"source": "NHK", "url": "..."}],
  "generatedAt": "2026-08-12T07:00:00+09:00",
  "aiGenerated": true
}
```

### 4.4 公開
- `/data/2026-08-12.json` としてコミット
- フロントはトップページで当日分をfetch、過去分は日付ナビゲーションで遡れるようにする

---

## 5. フロントエンド仕様(React単一ファイルPWA)

- 構成: 既存プロジェクト(苗字ルーツ帖など)と同じ単一ファイルReact + Vercel deploy
- トップ: 当日のトピック一覧(カード形式、論点タイトルのみ見せて詳細はクリックで展開)
- トピック詳細: 事実→論点→賛否(左右2カラムまたは上下に「賛成側/反対側」を配置)→出典リンク一覧
- 免責表示: 全ページ共通フッターに「本コンテンツはAIが自動生成しています。詳細は各出典元をご確認ください」を明示
- ソース透明性: 各要約の下に必ず参照メディア名をバッジ表示

---

## 6. 著作権・法務上の注意点

- 記事本文の引用は行わない。見出し・リード文からの要約・言い換えのみ
- 1つの論点内で同一媒体からの要約は簡潔に(2〜3文まで)
- 各トピックに必ず元記事へのリンクを設置(誘導であることを明示、著作権法上の「引用の目的上正当な範囲」を超えないよう要約主体にする)
- AI生成である旨を明示し、誤りがあり得る免責を掲載

---

## 7. 今後の実装ステップ

1. `collect.js`: RSS取得・正規化スクリプト
2. Claude APIプロンプトのプロトタイプ(クラスタリング→論点抽出を1トピックで試す)
3. GitHub Actions workflow(cron設定)
4. フロントエンドの雛形(トップ+詳細ページ)
5. Vercelデプロイ・API プロキシ設定(APIキー秘匿)
6. 初期は手動実行で数日分試し、出力品質を確認した上で完全自動cronに切り替える
